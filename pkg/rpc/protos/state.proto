syntax = "proto3";

package protos;

// Ping services definition.
service StatusController {
    rpc Ping (PingRequest) returns (PingReply) {
    }
}

message PingRequest {
    string message = 1;
}

message PingReply {
    string message = 1;
}
